<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_measurement_key"
        android:entries="@array/pref_weight_list_titles"
        android:entryValues="@array/pref_weight_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_measurement_title"
        android:summary="lb" />

    <Preference
        android:key="@string/pref_reset_data_key"
        android:negativeButtonText="Cancel"
        android:positiveButtonText="Reset"
        android:title="Reset Data" />

</PreferenceScreen>
